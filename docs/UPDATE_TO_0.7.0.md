# Update to v0.7.0 — Milestone 6

This is a complete repository package. Upload the contents of the unzipped folder to a fresh repository if that method remains the most reliable.

Verify before building:

- `package.json`: `0.7.0`
- `web/js/rules.js`: `APP_VERSION = "0.7.0"`
- `web/js/board.js`: present
- `web/index.html`: `Milestone 6 full board build`
- `src-tauri/tauri.conf.json`: `Native v0.7.0`

The board uses new vector art and a hand-reconstructed territory graph; it does not embed scans of the original board.
